import { Outlet, createFileRoute } from "@tanstack/react-router"; import { ReviewPage } from "@/pages/upload-review";
export const Route = createFileRoute("/review")({ head: () => ({ meta: [{ title: "Review Queue — BhuRakshak" }, { name: "description", content: "Review OCR-extracted land record fields and confidence scores." }, { property: "og:title", content: "Review Queue — BhuRakshak" }, { property: "og:description", content: "Review OCR-extracted land record fields and confidence scores." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary" }] }), component: Outlet });
