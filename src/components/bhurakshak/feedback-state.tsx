import { AlertTriangle, FileSearch, LoaderCircle } from "lucide-react";
import { Button } from "@/components/ui/button";
export function LoadingState({ label = "Loading records…" }: { label?: string }) { return <div className="grid min-h-48 place-items-center text-center"><div><LoaderCircle className="mx-auto size-6 animate-spin text-primary"/><p className="mt-3 text-sm text-muted-foreground">{label}</p></div></div>; }
export function EmptyState({ title = "No records found", description = "Try changing the filters or search terms.", action }: { title?: string; description?: string; action?: () => void }) { return <div className="grid min-h-56 place-items-center border border-dashed border-border bg-card text-center"><div className="max-w-sm p-6"><FileSearch className="mx-auto size-8 text-muted-foreground"/><h3 className="mt-3 font-semibold">{title}</h3><p className="mt-1 text-sm text-muted-foreground">{description}</p>{action && <Button variant="outline" size="sm" className="mt-4" onClick={action}>Clear filters</Button>}</div></div>; }
export function ErrorState() { return <div className="grid min-h-48 place-items-center border border-destructive/20 bg-danger-soft text-center"><div><AlertTriangle className="mx-auto size-7 text-destructive"/><h3 className="mt-3 font-semibold">Records could not be loaded</h3><p className="mt-1 text-sm text-muted-foreground">The demo data is temporarily unavailable.</p></div></div>; }
